export interface Satellite {
  id: string; //UUID
  name: string;
  // altitude: number;
  // velocity: number;
  // latitude: number;
  // longitude: number;
  // status: enum;
  // lastContactTime: time;
}

